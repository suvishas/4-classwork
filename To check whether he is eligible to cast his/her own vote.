#include<stdio.h>
int i;
int main() {
	printf("Enter Value of age : ");
	scanf("%d",&i);
	if(i>=18)
	printf("preson is eligible to vote");
	else 
	printf("preson is not eligible to vote");
	return 0;
}
